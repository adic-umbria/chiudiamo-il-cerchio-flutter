import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Empty test case', (WidgetTester tester) async {
    // This application is not tested. When more time is allocated to it,
    // we can provide production quality software.

    expect(42, 42);
  });
}
